# The directory `get-shit-done-for-antigravity` is tracked as a gitlink (mode 160000) but lacks a `.gitmodules` entry
git rm --cached get-shit-done-for-antigravity
git commit -m "Fix CI: remove invalid gitlink for get-shit-done-for-antigravity"
